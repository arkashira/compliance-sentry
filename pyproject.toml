[project]
name = "compliance-sentry"
version = "0.1.0"
description = "Real‑Time Audit‑Ready Logging"
requires-python = ">=3.8"

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-q"
testpaths = ["tests"]
